apiVersion: v1
kind: Service
metadata:
  labels:
    app: fi-mcp-gateway
    component: mcp-gateway
  name: fi-mcp-gateway
  namespace: mcp-hub
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: fi-mcp-gateway
  type: ClusterIP
